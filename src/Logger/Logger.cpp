#include <iostream>

#include "Logger.h"

// template <typename... Args> void Logger::Debug(const char* msg, Args... args) {
// 	if(logLevel <= ERROR) {
// 		fprintf(stdout, "[DEBUG]\t");
// 		fprintf(stdout, msg, args...);
// 	}
// }